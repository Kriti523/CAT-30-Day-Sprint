// Maps `import ... from 'react'` to the UMD global loaded from cdnjs.
const R = window.React;
export default R;
export const { useState, useEffect, useMemo, useCallback, useRef, createContext, useContext, StrictMode, Fragment, createElement } = R;
